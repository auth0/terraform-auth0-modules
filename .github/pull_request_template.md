## Summary

<!-- What does this PR do and why? -->

## Module(s) affected

<!-- e.g. modules/b2b-saas-starter -->

## Related example

<!-- Which example directory demonstrates this change? e.g. examples/b2b-saas-starter/case2 -->

## Checklist

- [ ] `terraform fmt -recursive` passes from repo root
- [ ] `terraform validate` passes on every affected `modules/*/` directory
- [ ] `terraform validate` passes on every affected `examples/*/*/` directory
- [ ] Resource counts in affected case `main.tf` comment headers are correct
- [ ] The module's own README Inputs/Outputs tables updated if variables or outputs changed
- [ ] CHANGELOG.md updated under `[Unreleased]`, grouped by module
